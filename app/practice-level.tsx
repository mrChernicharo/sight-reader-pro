import { AppText } from "@/components/atoms/AppText";
import { ScrollView } from "react-native-gesture-handler";

export default function PracticeLevelScreen() {
  return (
    <ScrollView>
      <AppText>Practice Level</AppText>
    </ScrollView>
  );
}
